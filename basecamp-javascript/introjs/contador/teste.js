/*
Isto é um comentário
*/


// isto é um comentário 

// Comentando blando
// Xando gando

//var preco = 2;
var desconto = 0.2;
const PRECO = 50;
var muda = 23;

//preco = preco - desconto;

muda = muda - 10;
console.log(muda);

var desconto = PRECO * desconto;

console.log(desconto);